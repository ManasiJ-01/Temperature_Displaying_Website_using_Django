from django.contrib import admin
from users.models import Detail
from users.models import TempDisplay

# Register your models here.
admin.site.register(Detail)
admin.site.register(TempDisplay)
